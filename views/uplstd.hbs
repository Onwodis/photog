<div class="main-panel">
    <div class="content-wrapper">
        <div class="container">
            
            <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    

                    <div class="card-body">
                        <div style="display: flex;justify-content:end;">
                            <div class="w-45">
                                <p>Parent Sign up code</p>
                                <h6 class="text-success" id="clipr">copied to clipboard </h6>
                                
                                <span class="btn btn-warning px-3 py-2 text-capitalize ccopy" onclick="clickpar()">
                                    Copy: <span id="clickpar">{{student.signparent}}</span>
                                </span>
                                
                                <a href="/admin/schoolcode/{{student.userid}}" class="btn btn-success fs-30 px-3 py-2 text-capitalize">
                                    Hash new code
                                </a>
                                
                                {{!-- <p class="card-description">Upload page</p> --}}
                            </div>
                        </div>
                        <h4 class="text-capitalize">{{student.schoolname}}</h4>
                        
                        <h4 class="card-title">{{student.classs}}</h4>

                        
                        <form class="forms-sample" enctype="multipart/form-data" action="/admin/uploads" method="post">

                            
                            <div class="form-group">
                                <label>Add image</label>
                                <input type="file" name="imga" class="form-control form-control-file"
                                    placeholder="Upload Image" accept="image/jpg,image/jpeg,image/gif,image/png,image/svg" onchange="this.form.submit()" required/>
                            </div>

                            {{!-- <div class="form-group">
                                <label>Add image</label>
                                <input type="file" name="imgb" class="form-control form-control-file"
                                    placeholder="Upload Image" accept="image/jpg,image/jpeg,image/gif,image/png,image/svg" />
                            </div>

                            <div class="form-group">
                                <label>Add image</label>
                                <input type="file" name="imgc" class="form-control form-control-file"
                                    placeholder="Upload Image" accept="image/jpg,image/jpeg,image/gif,image/png,image/svg" />
                            </div>

                            <div class="form-group">
                                <label>Add image</label>
                                <input type="file" name="imgd" class="form-control form-control-file"
                                    placeholder="Upload Image" accept="image/jpg,image/jpeg,image/gif,image/png,image/svg" />
                            </div>

                            <button type="submit" class="btn btn-primary mr-2">
                                Submit
                            </button>
                            <button class="btn btn-light">Cancel</button> --}}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>